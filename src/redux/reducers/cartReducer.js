import { TYPES } from "../actions/types.js";

const initialState = {
  cart:[]
};

// function cartReducer(state=initialState,action){
//   switch (action.type) {
//     case TYPES:
      
//       return({

//       })
  
//     default:
//       return state;
//   }
// }